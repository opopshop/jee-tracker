<manifest package="com.jeetracker.app" xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:name=".JeeTrackerApp"
        android:allowBackup="true"
        android:label="JEE Tracker"
        android:theme="@style/Theme.JeeTracker">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".sync.SyncService" android:exported="false" />
    </application>
</manifest>
